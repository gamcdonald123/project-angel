<div class="container">
  <div class="row">
    <div class="col">

    <h1 id="community_name"><%= @community.name %></h1>

    <% @community.posts.each do |post| %>

      <%= link_to community_post_path(@community, post) do %>
<%# This posts class is also used in the posts show view %>
      <div class="posts">

        <p id="title"><%= post.title %></p>
        <p id="content"><%= post.content.truncate(50) %></p>
        <%# <p>Sent by <%= post.user.first_name %>

        </div>
      <% end %>
    <% end %>


<br>
<br>

<div class="btn btn-dark w-100 rounded-4 mb-3" data-bs-toggle="modal" data-bs-target="#exampleModal">
  <p>Add Post</p>
</div>

<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">What would you like to say?</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">

        <%= simple_form_for [@community, @post] do |f| %>
          <%= f.input :title %>
          <%= f.input :content %>
          <%= f.button :submit, class: "btn btn-dark rounded-4" %>
        <% end %>


      </div>
      <%# <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary">Save changes</button>
      </div> %>
    </div>
  </div>
</div>


  <div class="btn btn-dark w-100 rounded-4">
    <p><%= link_to "Back", communities_path %></p>
  </div>

</div>
</div>
</div>
