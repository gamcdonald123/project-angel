<div id="message-<%= message.id %>">
  <p style="font-size: 15px; text-align: left; "><%= message.content %></p>
  <%# <small class = "text-right"> %>
    <p style="text-align: right; margin-bottom: 0px;">
      <strong style="font-size: 13px;"><%= message.user.first_name %></strong>
      <i style="font-size: 13px;"><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
    </p>
  <%# </small> %>
</div>
